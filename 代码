# PiStar0.6 / AssembleBlock1 当前保守版流程

目标：
- 原始 500 条成功 demo 只抽 250 条，先训练一个初始 pi0.5 policy。
- 用这个初始 policy 做 DAgger rollout，采 250 条：200 成功、50 失败，其中允许 SpaceMouse 干预。
- 用这 250 条 DAgger 数据训练 value function。
- 用 value function 给同一批 DAgger 数据打 advantage。
- 在内网 H20 上从 pi0.5 base 训练 PiStar policy。
- 回本地真机推理/rollout 测成功率。

重要路径：

```bash
# 本地原始 500 条 demo
/home/user/.cache/huggingface/lerobot/piper/assemble_block1_v21

# 本地抽出来的 250 条 demo，用来训练初始 pi0.5
/home/user/.cache/huggingface/lerobot/piper/assemble_block1_v21_demo250_uniform

# 本地 DAgger rollout 250 条，200 成功 / 50 失败
/home/user/.cache/huggingface/lerobot/piper/assemble_block1_demo250_dagger_r0

# 本地 value checkpoint
/home/user/code/pistar/checkpoints/value/assemble_block1_dagger250_s200_f50_accum4_r0/step_00010000

# 本地已经打好 advantage 的 DAgger 数据集，后续 policy 训练用这个
/home/user/.cache/huggingface/lerobot/piper/assemble_block1_demo250_dagger_r0_adv_value_accum4_r0
```

相机：

```bash
head=323522063521
side=349222061138
wrist=409122272461
```

任务语言：

```bash
Pick up the block1 and assemble it.
```

---

## 1. 从 500 条 demo 抽 250 条

在本地 pistar：

```bash
cd /home/user/code/pistar

PYTHONPATH=src venv/bin/python scripts/sample_lerobot_episode_subset.py \
  --source /home/user/.cache/huggingface/lerobot/piper/assemble_block1_v21 \
  --output /home/user/.cache/huggingface/lerobot/piper/assemble_block1_v21_demo250_uniform \
  --count 250 \
  --strategy stratified \
  --seed 42 \
  --video-mode copy \
  --overwrite
```

说明：
- `stratified` 是按 episode 顺序分层抽样，不是只拿前 250 条。
- `--video-mode copy` 是严格实验用，数据集独立，不依赖原始 500 条的软链接。
- 如果只是本机快速试，也可以把 `copy` 换成 `symlink`，但传内网不建议。

---

## 2. 用 250 条 demo 训练初始 pi0.5 policy

这一步在 openpi docker / openpi 代码里做，不在 pistar 里做。

如果是在内网 openpi docker：

```bash
docker exec -it openpi_test /bin/bash
cd /app

export HF_LEROBOT_HOME=/workspaces/lerobot
export HF_HUB_OFFLINE=1
export HF_DATASETS_OFFLINE=1
export TRANSFORMERS_OFFLINE=1
export XLA_PYTHON_CLIENT_PREALLOCATE=false
export HF_DATASETS_CACHE=/tmp/hf_datasets_cache_pi05_demo250
export JAX_COMPILATION_CACHE_DIR=/tmp/jax_cache
```

先确认你的 openpi `config.py` 里训练配置的 `repo_id` 指向：

```bash
/home/user/.cache/huggingface/lerobot/piper/assemble_block1_v21_demo250_uniform
```

算 norm：

```bash
PYTHONPATH=src python scripts/compute_norm_stats.py \
  --config-name pi05_Piper_AssembleBlock1 \
  --repo-id /home/user/.cache/huggingface/lerobot/piper/assemble_block1_v21_demo250_uniform
```

训练初始 pi0.5：

```bash
PYTHONPATH=src python scripts/train.py pi05_Piper_AssembleBlock1 \
  --exp-name pistar_AssembleBlock1_250 \
  --data.repo-id /home/user/.cache/huggingface/lerobot/piper/assemble_block1_v21_demo250_uniform \
  --weight-loader.params-path /root/data_local/pi05_base/pi05base/params \
  --batch-size 192 \
  --fsdp-devices 6 \
  --num-workers 8 \
  --num-train-steps 15000 \
  --save-interval 1000 \
  --keep-period 1000 \
  --no-wandb-enabled \
  --overwrite
```

已经可用的本地初始 policy checkpoint：

```bash
/home/user/code/pistar/checkpoints/pistar_AssembleBlock1_250/11000
```

---

## 3. 本地启动初始 pi0.5 policy server

如果 checkpoint 是 openpi 格式，用 openpi serve：

```bash
cd /home/user/code/openpi

export XLA_PYTHON_CLIENT_PREALLOCATE=false

uv run scripts/serve_policy.py policy:checkpoint \
  --policy.config=pi05_Piper_AssembleBlock1_inference \
  --policy.dir=/home/user/code/pistar/checkpoints/pistar_AssembleBlock1_250/11000 \
  --port=8000
```

注意：
- 如果这个 config 仍然读 `/home/user/.cache/huggingface/lerobot/piper/assemble_block1_v21/norm_stats.json`，就要把 250 条 demo 的 norm 放到 config 对应路径，或者在 openpi config 里把 inference repo 改成 `assemble_block1_v21_demo250_uniform`。
- policy server 开着不要关，下面 DAgger rollout 会连 `localhost:8000`。

---

## 4. 用 SpaceMouse 做 DAgger rollout，采 250 条

目标：
- 250 条总数
- 200 成功
- 50 失败
- 干预后该 episode 后续一直标 intervention=1
- `h` 可以 home，home 以后也算干预
- 最长 1500 帧

在本地 pistar：

```bash
cd /home/user/code/pistar

scripts/run_pi05_rtc_rollout_collect.sh \
  --control-repo-path /home/user/code/control_your_robot \
  --server-host localhost \
  --server-port 8000 \
  --repo-id assemble_block1_demo250_dagger_r0 \
  --output-dir /home/user/.cache/huggingface/lerobot/piper \
  --task-name "Pick up the block1 and assemble it." \
  --arm-can can0 \
  --arm-name left_arm \
  --state-source joint \
  --cam-head-serial 323522063521 \
  --cam-side-serial 349222061138 \
  --cam-wrist-serial 409122272461 \
  --fps 30 \
  --control-dt 0.033333 \
  --action-horizon 50 \
  --num-episode 250 \
  --max-step 1500 \
  --save-adv-ind none \
  --enter-label discard \
  --min-save-frames 30 \
  --post-reset-sleep 2.0 \
  --enable-spacemouse-intervention \
  --sticky-intervention true \
  --spacemouse-control-hz 200 \
  --rtc-enabled true \
  --rtc-execution-horizon 10 \
  --rtc-max-guidance-weight 10.0 \
  --rtc-prefix-attention-schedule exp \
  --rtc-measure-inference-delay false \
  --rtc-inference-delay-steps 4 \
  --rtc-prefetch-threshold 20 \
  --rtc-worker-sleep 0.005 \
  --rtc-debug false
```

采集按键：

```text
Enter  开始
s      成功保存
f      失败保存
r      丢弃当前 episode，不保存
h      home，并从此 episode 后续都算干预
q/Esc  退出
```

当前这次已经采完：

```text
saved_total=250
success=200
failure=50
dagger=64
dagger_rate=25.6%
intervention_frames=18733
discarded=37
```

---

## 5. 本地训练 value function

这一步用本地 4090 训练即可，冻结 SigLIP + Gemma，只训练 value head。

```bash
cd /home/user/code/pistar

export HF_DATASETS_CACHE=/tmp/hf_datasets_cache_dagger250_value
export JAX_COMPILATION_CACHE_DIR=/tmp/jax_cache
export XLA_PYTHON_CLIENT_PREALLOCATE=false

PYTHONPATH=src venv/bin/python scripts/train_value.py \
  --data_dir /home/user/.cache/huggingface/lerobot/piper/assemble_block1_demo250_dagger_r0 \
  --checkpoint_dir /home/user/code/pistar/checkpoints/value/assemble_block1_dagger250_s200_f50_accum4_r0 \
  --batch_size 4 \
  --gradient_accumulation_steps 4 \
  --num_train_steps 10000 \
  --log_interval 100 \
  --save_interval 2500 \
  --num_workers 0 \
  --wandb_mode disabled \
  --tokenizer_path /home/user/hf_models/ybpy/vlm_ckpt/tokenizer.model \
  --siglip_checkpoint_path /home/user/hf_models/ybpy/vlm_ckpt/siglip2-so400m-patch14-224-jax/siglip2_so400m14_224.npz \
  --gemma_checkpoint_dir /home/user/hf_models/ybpy/vlm_ckpt/gemma-3-270m \
  --freeze_mode all_backbones \
  --load_pretrained
```

当前可用 checkpoint：

```bash
/home/user/code/pistar/checkpoints/value/assemble_block1_dagger250_s200_f50_accum4_r0/step_00010000
```

---

## 6. 快速评估 value function

先只看每个 episode 终点：

```bash
cd /home/user/code/pistar

export XLA_PYTHON_CLIENT_PREALLOCATE=false
export JAX_COMPILATION_CACHE_DIR=/tmp/jax_cache

PYTHONPATH=src venv/bin/python scripts/export_vlm_values.py \
  --data_dir /home/user/.cache/huggingface/lerobot/piper/assemble_block1_demo250_dagger_r0 \
  --checkpoint_dir /home/user/code/pistar/checkpoints/value/assemble_block1_dagger250_s200_f50_accum4_r0 \
  --checkpoint_name step_00010000 \
  --output_path outputs/assemble_block1_dagger250_value_step10000_terminal.parquet \
  --tokenizer_path /home/user/hf_models/ybpy/vlm_ckpt/tokenizer.model \
  --batch_size 4 \
  --num_workers 0 \
  --terminal_only
```

这次结果：

```text
success terminal mean ~= -0.415
failure terminal mean ~= -0.586
AUC ~= 0.799
```

可视化两个普通样本：

```bash
PYTHONPATH=src venv/bin/python scripts/export_vlm_values.py \
  --data_dir /home/user/.cache/huggingface/lerobot/piper/assemble_block1_demo250_dagger_r0 \
  --checkpoint_dir /home/user/code/pistar/checkpoints/value/assemble_block1_dagger250_s200_f50_accum4_r0 \
  --checkpoint_name step_00010000 \
  --output_path outputs/assemble_block1_dagger250_value_step10000_ep186_217.parquet \
  --tokenizer_path /home/user/hf_models/ybpy/vlm_ckpt/tokenizer.model \
  --batch_size 4 \
  --num_workers 0 \
  --episode_ids 186 217

MPLCONFIGDIR=/tmp/matplotlib-pistar PYTHONPATH=src venv/bin/python scripts/render_episode_value_video.py \
  --values_path outputs/assemble_block1_dagger250_value_step10000_ep186_217.parquet \
  --output_dir outputs/value_videos_dagger250_step10000 \
  --camera_column side_image \
  --stride 2
```

可视化两个极端样本：

```bash
PYTHONPATH=src venv/bin/python scripts/export_vlm_values.py \
  --data_dir /home/user/.cache/huggingface/lerobot/piper/assemble_block1_demo250_dagger_r0 \
  --checkpoint_dir /home/user/code/pistar/checkpoints/value/assemble_block1_dagger250_s200_f50_accum4_r0 \
  --checkpoint_name step_00010000 \
  --output_path outputs/assemble_block1_dagger250_value_step10000_ep185_204.parquet \
  --tokenizer_path /home/user/hf_models/ybpy/vlm_ckpt/tokenizer.model \
  --batch_size 4 \
  --num_workers 0 \
  --episode_ids 185 204

MPLCONFIGDIR=/tmp/matplotlib-pistar PYTHONPATH=src venv/bin/python scripts/render_episode_value_video.py \
  --values_path outputs/assemble_block1_dagger250_value_step10000_ep185_204.parquet \
  --output_dir outputs/value_videos_dagger250_step10000 \
  --camera_column side_image \
  --stride 2
```

视频输出：

```text
outputs/value_videos_dagger250_step10000/episode_000185_success_side_image_value.mp4
outputs/value_videos_dagger250_step10000/episode_000186_success_side_image_value.mp4
outputs/value_videos_dagger250_step10000/episode_000204_failure_side_image_value.mp4
outputs/value_videos_dagger250_step10000/episode_000217_failure_side_image_value.mp4
```

---

## 7. 用 value function 给 DAgger 数据打 advantage

先复制一份，不要改原始 DAgger 数据：

```bash
cd /home/user/code/pistar

SRC=/home/user/.cache/huggingface/lerobot/piper/assemble_block1_demo250_dagger_r0
ADV=/home/user/.cache/huggingface/lerobot/piper/assemble_block1_demo250_dagger_r0_adv_value_accum4_r0

test ! -e "$ADV" && cp -a "$SRC" "$ADV"
```

打 advantage：

```bash
HF_DATASETS_CACHE=/tmp/hf_datasets_cache_dagger250_value \
MPLCONFIGDIR=/tmp/matplotlib-pistar \
XLA_PYTHON_CLIENT_PREALLOCATE=false \
JAX_COMPILATION_CACHE_DIR=/tmp/jax_cache \
PYTHONPATH=src venv/bin/python scripts/label_advantage_from_vlm.py \
  --data_dir /home/user/.cache/huggingface/lerobot/piper/assemble_block1_demo250_dagger_r0_adv_value_accum4_r0 \
  --checkpoint_dir /home/user/code/pistar/checkpoints/value/assemble_block1_dagger250_s200_f50_accum4_r0 \
  --checkpoint_name step_00010000 \
  --tokenizer_path /home/user/hf_models/ybpy/vlm_ckpt/tokenizer.model \
  --batch_size 4 \
  --num_workers 0 \
  --lookahead 50 \
  --top_percent 30 \
  --right_wrist_image_col side_image
```

这次实际写入结果：

```text
episodes=250
frames=160410
positive=61236
negative=99174
intervention_frames=18733
positive_intervention=18733
```

含义：
- 所有 SpaceMouse 干预帧直接标 `positive`。
- 非干预帧按 VLM value advantage 取 top 30% 为 `positive`。
- 剩下为 `negative`。

---

## 8. 打包 advantage 数据集到内网

本地打包：

```bash
mkdir -p /home/user/pistar_transfer

tar -I 'zstd -T0 -3' \
  -cf /home/user/pistar_transfer/assemble_block1_demo250_dagger_r0_adv_value_accum4_r0.tar.zst \
  -C /home/user/.cache/huggingface/lerobot/piper \
  assemble_block1_demo250_dagger_r0_adv_value_accum4_r0
```

内网解压：

```bash
mkdir -p /root/data_local/lerobot/piper

zstd -t /root/docker/assemble_block1_demo250_dagger_r0_adv_value_accum4_r0.tar.zst

tar -I 'zstd -T0 -d' \
  -xf /root/docker/assemble_block1_demo250_dagger_r0_adv_value_accum4_r0.tar.zst \
  -C /root/data_local/lerobot/piper
```

容器里应该能看到：

```bash
/home/user/.cache/huggingface/lerobot/piper/assemble_block1_demo250_dagger_r0_adv_value_accum4_r0
```

---

## 9. 内网 H20 训练 PiStar policy

如果已有 `pistar_train` 容器：

```bash
docker exec -it pistar_train /bin/bash
cd /home/user/code/pistar
```

如果要重新开容器，用 6 张 H20：

```bash
docker run -t -d \
  --shm-size 256G \
  --restart=always \
  --runtime=nvidia \
  --name=pistar_train \
  -e NVIDIA_VISIBLE_DEVICES=0,1,2,3,4,5 \
  -e NVIDIA_DRIVER_CAPABILITIES=compute,utility \
  -e XLA_PYTHON_CLIENT_PREALLOCATE=false \
  -e HF_HUB_OFFLINE=1 \
  -e HF_DATASETS_OFFLINE=1 \
  -e TRANSFORMERS_OFFLINE=1 \
  -v /root/data_local/:/workspaces \
  -v /root/data_local/lerobot:/home/user/.cache/huggingface/lerobot \
  -v /root/data_local/pistar_runs:/workspace/pistar/checkpoints \
  -v /root/data_local/pistar_assets:/workspace/pistar/assets \
  -v /root/data_local/pi05_base/pi05base:/mnt/pi05_base \
  pistar-train-venv:38f7f38 \
  /bin/bash
```

环境变量：

```bash
cd /home/user/code/pistar

export HF_LEROBOT_HOME=/workspaces/lerobot
export HF_HUB_OFFLINE=1
export HF_DATASETS_OFFLINE=1
export TRANSFORMERS_OFFLINE=1
export XLA_PYTHON_CLIENT_PREALLOCATE=false
export HF_DATASETS_CACHE=/tmp/hf_datasets_cache_dagger250_adv
export JAX_COMPILATION_CACHE_DIR=/tmp/jax_cache
```

算 norm：

```bash
CUDA_VISIBLE_DEVICES=0 \
PYTHONPATH=src python scripts/compute_norm_stats.py \
  --config-name pi05_star_assemble_blocks_h50_from_pi05 \
  --repo-id piper/assemble_block1_demo250_dagger_r0_adv_value_accum4_r0 \
  --local-data-dir /workspaces/lerobot/piper/assemble_block1_demo250_dagger_r0_adv_value_accum4_r0
```

复制 norm 到统一 assets 目录：

```bash
mkdir -p /workspace/pistar/assets/pi05_star_assemble_blocks_h50_from_pi05/piper

cp -a \
  assets/pi05_star_assemble_blocks_h50_from_pi05/piper/assemble_block1_demo250_dagger_r0_adv_value_accum4_r0 \
  /workspace/pistar/assets/pi05_star_assemble_blocks_h50_from_pi05/piper/
```

从 pi0.5 base 训练 PiStar：

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3,4,5 \
PYTHONPATH=src python scripts/train.py pi05_star_assemble_blocks_h50_from_pi05 \
  --exp-name assemble_block1_pistar_dagger250_adv_pi05base_h20_6gpu_b192_r1 \
  --data.repo-id piper/assemble_block1_demo250_dagger_r0_adv_value_accum4_r0 \
  --assets-base-dir /workspace/pistar/assets \
  --checkpoint-base-dir /workspace/pistar/checkpoints \
  --weight-loader.params-path /mnt/pi05_base/params \
  --batch-size 192 \
  --fsdp-devices 6 \
  --num-workers 8 \
  --num-train-steps 30000 \
  --save-interval 5000 \
  --keep-period 5000 \
  --no-wandb-enabled \
  --overwrite
```

注意：
- `--batch-size 192` 是总 batch，6 卡时约每卡 32。
- `--weight-loader.params-path /mnt/pi05_base/params` 是从 pi0.5 base 开始，不是从 40000 full-finetune 开始。
- 先看 `5000 / 10000 / 15000`，不要只等 `30000`。

输出 checkpoint：

```bash
/workspace/pistar/checkpoints/pi05_star_assemble_blocks_h50_from_pi05/assemble_block1_pistar_dagger250_adv_pi05base_h20_6gpu_b192_r1/5000
/workspace/pistar/checkpoints/pi05_star_assemble_blocks_h50_from_pi05/assemble_block1_pistar_dagger250_adv_pi05base_h20_6gpu_b192_r1/10000
/workspace/pistar/checkpoints/pi05_star_assemble_blocks_h50_from_pi05/assemble_block1_pistar_dagger250_adv_pi05base_h20_6gpu_b192_r1/15000
```

---

## 10. 拷回本地推理

假设拷回本地路径：

```bash
/home/user/code/pistar/checkpoints/assemble_block1_pistar_dagger250_adv_pi05base_h20_6gpu_b192_r1/5000
```

启动 PiStar policy server：

```bash
cd /home/user/code/pistar

export XLA_PYTHON_CLIENT_PREALLOCATE=false

PYTHONPATH=src venv/bin/python scripts/serve_policy.py policy:checkpoint \
  --policy.config=pi05_star_assemble_blocks_h50_from_pi05_infer \
  --policy.dir=/home/user/code/pistar/checkpoints/assemble_block1_pistar_dagger250_adv_pi05base_h20_6gpu_b192_r1/5000 \
  --port=8000
```

注意：
- 推理 config 是 `pi05_star_assemble_blocks_h50_from_pi05_infer`。
- 这个 infer config 里 `adv_guidance_beta=2.0`，会启用 advantage guidance。
- policy rollout 时要传 `--adv-ind positive`。

---

## 11. 本地真机 rollout 评估 PiStar

只评估，不保存数据：

```bash
cd /home/user/code/pistar

scripts/run_pi05_rtc_success_eval.sh \
  --control-repo-path /home/user/code/control_your_robot \
  --server-host localhost \
  --server-port 8000 \
  --task-name "Pick up the block1 and assemble it." \
  --adv-ind positive \
  --arm-can can0 \
  --arm-name left_arm \
  --state-source joint \
  --cam-head-serial 323522063521 \
  --cam-side-serial 349222061138 \
  --cam-wrist-serial 409122272461 \
  --fps 30 \
  --control-dt 0.033333 \
  --action-horizon 50 \
  --max-step 1500 \
  --num-id 30 \
  --num-position-ood 10 \
  --num-angle-ood 10 \
  --post-reset-sleep 2.0 \
  --rtc-enabled true \
  --rtc-execution-horizon 10 \
  --rtc-max-guidance-weight 10.0 \
  --rtc-prefix-attention-schedule exp \
  --rtc-measure-inference-delay false \
  --rtc-inference-delay-steps 4 \
  --rtc-prefetch-threshold 20 \
  --rtc-worker-sleep 0.005 \
  --rtc-debug false
```

如果要保存 rollout 数据，方便后续继续迭代：

```bash
cd /home/user/code/pistar

scripts/run_pi05_rtc_rollout_collect.sh \
  --control-repo-path /home/user/code/control_your_robot \
  --server-host localhost \
  --server-port 8000 \
  --repo-id assemble_block1_pistar_dagger250_eval_5000 \
  --output-dir /home/user/.cache/huggingface/lerobot/piper \
  --task-name "Pick up the block1 and assemble it." \
  --adv-ind positive \
  --arm-can can0 \
  --arm-name left_arm \
  --state-source joint \
  --cam-head-serial 323522063521 \
  --cam-side-serial 349222061138 \
  --cam-wrist-serial 409122272461 \
  --fps 30 \
  --control-dt 0.033333 \
  --action-horizon 50 \
  --num-episode 50 \
  --max-step 1500 \
  --save-adv-ind positive \
  --enter-label discard \
  --min-save-frames 30 \
  --post-reset-sleep 2.0 \
  --rtc-enabled true \
  --rtc-execution-horizon 10 \
  --rtc-max-guidance-weight 10.0 \
  --rtc-prefix-attention-schedule exp \
  --rtc-measure-inference-delay false \
  --rtc-inference-delay-steps 4 \
  --rtc-prefetch-threshold 20 \
  --rtc-worker-sleep 0.005 \
  --rtc-debug false
```

---

## 12. 当前建议

优先顺序：

```text
1. 内网训 5000 step
2. 拷回本地测 20-50 次
3. 如果比初始 pi0.5 差，先停，不要继续 30000
4. 如果接近或更好，再测 10000 / 15000
5. 最后再决定要不要继续收集第二轮 DAgger
```

这次最关键的判断不是训练 loss，而是真机 rollout 成功率。
